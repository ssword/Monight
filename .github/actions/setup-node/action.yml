name: Set up Node.js dependencies
description: Install the Node.js toolchain and locked project dependencies used by CI and packaging

runs:
  using: composite
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        node-version: 24
        cache: npm
    - name: Install dependencies
      shell: bash
      run: npm ci
